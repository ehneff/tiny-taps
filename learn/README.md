# learn/

Learning-oriented games go here, each in its own folder (see `calm/soft-bubbles/` for the pattern to follow: a self-contained `.html` file plus its own `manifest.json` and icons).

No games here yet.
